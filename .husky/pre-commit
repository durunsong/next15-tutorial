#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 运行 lint-staged 检查
npx lint-staged

# 检查 TypeScript 类型
echo "🔍 Running TypeScript type check..."
pnpm type-check

echo "✅ Pre-commit checks passed!"
